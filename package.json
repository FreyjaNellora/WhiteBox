{
  "private": true,
  "type": "module",
  "workspaces": [
    "whitebox-shared",
    "whitebox-mcp",
    "whitebox-cli"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "npm test --workspaces --if-present"
  },
  "devDependencies": {
    "vitest": "^3.1.0"
  }
}
